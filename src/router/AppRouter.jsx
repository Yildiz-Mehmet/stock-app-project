import React from "react";
import { Router, Routes } from "react-router-dom";

const AppRouter = () => {
  return (
    <Router>
      <Routes></Routes>
    </Router>
  );
};

export default AppRouter;
